{
  "name": "steedos-templates",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "steedos-packages/*"
  ],
  "scripts": {
    "start:db": "docker-compose up mongodb redis",
    "start": "steedos start",
    "build": "lerna run build"
  },
  "dependencies": {
    "@steedos/server": "^3.0.4"
  },
  "devDependencies": {
    "lerna": "^5.6.2",
    "typescript": "^4.9.3"
  }
}
