{
  "name": "steedos-templates",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "steedos-packages/*"
  ],
  "scripts": {
    "start:db": "docker-compose up mongodb redis",
    "start": "steedos start",
    "build": "lerna run build",
    "release": "lerna publish --force-publish --exact --registry=https://registry.npmjs.org/"
  },
  "dependencies": {
    "@steedos/server": "^3.0.9-beta.1"
  },
  "devDependencies": {
    "lerna": "^5.6.2",
    "typescript": "^4.9.3"
  }
}
