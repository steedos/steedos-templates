{
  "name": "My Project",
  // 你的 image 和其余配置...
  "customizations": {
    "vscode": {
      "tasks": [
        {
          "label": "DB",
          "type": "shell",
          "command": "yarn start:db",
          "problemMatcher": []
        },
        {
          "label": "Steedos",
          "type": "shell",
          "command": "yarn && yarn build && yarn start",
          "problemMatcher": []
        },
      ],
      "settings": {
        // 可选项，设定默认打开的terminal
      },
      "extensions": [
        // 推荐安装的VS Code扩展
      ]
    }
  },
  "postCreateCommand": "yarn install && yarn build"
}